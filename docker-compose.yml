# QuickMart Shared Infrastructure Services
# This docker-compose provides shared network for microservices
# Note: Aerospike is now using Aerospike Cloud (managed service)
# Configure via environment variables: AEROSPIKE_HOST, AEROSPIKE_PORT, AEROSPIKE_TLS_*

networks:
  quickmart_shared_network:
    driver: bridge
    name: quickmart_shared_network
